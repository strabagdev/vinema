import { LegacyRouteRedirect } from "@/components/app-shell/legacy-route-redirect";

export default function InboxPage() {
  return (
    <LegacyRouteRedirect
      heading="Inicio"
      message="Vinema ahora empieza en una unica superficie de escritura."
    />
  );
}
